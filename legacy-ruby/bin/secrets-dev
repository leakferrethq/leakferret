#!/usr/bin/env ruby
# frozen_string_literal: true

# CLI entry point.
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'secrets_dev'
require 'secrets_dev/cli'

SecretsDev::CLI.start(ARGV)
